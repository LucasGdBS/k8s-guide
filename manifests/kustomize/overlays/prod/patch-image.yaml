apiVersion: apps/v1
kind: Deployment
metadata:
  name: web
  namespace: laboratorio
spec:
  template:
    spec:
      containers:
        - name: web        # <- faltou isso
          image: nginx:1.26